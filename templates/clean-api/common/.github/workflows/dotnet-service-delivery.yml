name: Company.SampleService Delivery To AKS

on:
  workflow_run:
    workflows: ["Company.SampleService CI"]
    branches: [main]
    types: [completed]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    uses: Flaviojcf/flaviojcf-pipelines/.github/workflows/dotnet-service-delivery.yml@main
    with:
      service_name: company-sample-service
      image_name: company-sample-service
      dockerfile_path: src/Company.SampleService.WebApi/Dockerfile
      docker_context: .
      azure_container_registry_name: ${{ vars.AZURE_CONTAINER_REGISTRY_NAME }}
      aks_resource_group: ${{ vars.AKS_RESOURCE_GROUP }}
      aks_cluster_name: ${{ vars.AKS_CLUSTER_NAME }}
      k8s_namespace: company-sample-service
      k8s_deployment_name: company-sample-service
      k8s_container_name: company-sample-service
      k8s_manifests_path: ""
      environment: production
      healthcheck_url: ""
    secrets: inherit
