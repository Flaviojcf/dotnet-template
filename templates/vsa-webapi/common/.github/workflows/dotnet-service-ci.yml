name: .NET Worker CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  ci:
    uses: Flaviojcf/flaviojcf-pipelines/.github/workflows/dotnet-service-ci.yml@main
    with:
      service-name: company-sample-service
      solution-path: Company.SampleService.slnx
