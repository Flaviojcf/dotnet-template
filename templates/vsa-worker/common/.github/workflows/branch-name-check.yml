name: Branch Name Check

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  branch-name-check:
    uses: Flaviojcf/flaviojcf-pipelines/.github/workflows/branch-name-check.yml@main
